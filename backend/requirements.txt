# Django core
Django==4.2.30
gunicorn==22.0.0

# PostgreSQL driver
psycopg2-binary==2.9.9

# Environment variables
python-dotenv==1.2.2

# Anthropic API
anthropic==0.49.0

# Voice mode requirements
# TTS engine (Supertonic-3). Pulls onnxruntime + numpy + soundfile + huggingface-hub.
# No PyTorch. 2.0.0 does not exist — 1.3.1 is the latest.
supertonic==1.3.1
# STT engine (Whisper). Transcribes the mic audio into the input box. Uses PyAV
# (bundled ffmpeg) to decode; ffmpeg is also installed in the Dockerfile as a safety net.
faster-whisper==1.2.1

# Redis (for session tracking and security attempt counters)
redis==5.0.3

# Encryption — conversation content encrypted at rest
cryptography==48.0.1

# Login brute force protection
django-axes==7.0.0

# Admin 2FA — TOTP via Google Authenticator
django-two-factor-auth==1.17.0
qrcode[pil]==7.4.2
phonenumbers==8.13.50

# Production only
dj-database-url==2.1.0
whitenoise==6.6.0
